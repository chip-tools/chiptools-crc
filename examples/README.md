# Examples

Runnable usage examples. Install the package first (from the repo root):

```bash
pip install -e .
```

Then run:

```bash
python examples/compute_crc.py   # compute CRCs from the catalogue and a custom model
python examples/audit_dump.py    # read-only integrity audit (coherent + tamper detection)
```
